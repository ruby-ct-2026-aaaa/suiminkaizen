# 峰小輔プロジェクト — CLAUDE.md

## ゲーム概要

ショートスリーパーのキャラクター「峰小輔」を、1週間（7日間）のあいだ強制睡眠させずに生き延びさせれば勝利するゲーム。
見た目はドット絵だが、疑似3D表現（奥行きのあるスプライト表示）に対応する。

企画・ミニゲームのアイデアは他のチームメンバーが `docs/design/` 以下に随時追加する。実装担当（Claude Code）はこのCLAUDE.mdと`docs/design/`配下の仕様書を仕様の正としてコードに落とし込む。

## 技術スタック

- 言語: Ruby
- ゲームライブラリ: Gosu
- テスト: Minitest（Ruby標準ライブラリに同梱されているため、`gem install`や`bundle install`なしで動く。描画に依存しないロジック部分は必ずテストを書く）
- Gosuは2D描画ライブラリでありフル3D投影は持たない。疑似3Dは「スプライトのスケーリング（奥行きに応じて`draw_rot`の拡大率を変える）」＋「視差スクロールする背景レイヤー」の組み合わせで実現する方針。レイキャスティング的な床描画を採用する場合はここに追記する。
- Gosu自体はネイティブ拡張を含むgemで、環境によっては`bundle install`が失敗しやすい。`lib/core`配下のロジックはGosuに依存しないため、Gosuのインストールでつまずいても`ruby`コマンド単体でテストを実行して開発を進められる。

## ディレクトリ構成

```
lib/
  core/         # 描画・入力に依存しない純粋なゲームロジック（睡眠ゲージ管理など）
  minigames/    # 各ミニゲームの実装。共通インターフェースに従う
  states/       # 画面・シーン管理（タイトル、1日のプレイ画面、リザルト等）
  rendering/    # 疑似3D描画まわりのユーティリティ（スケーリング計算、視差レイヤー）
main.rb         # Gosu::Windowのエントリーポイント
test/
  core/         # lib/core 配下のMinitestテスト（`ruby test/core/xxx_test.rb`で実行可能）
docs/
  design/       # 企画メンバーが書くミニゲーム仕様書、フローチャート等
```

`lib/core` 配下はGosuに一切依存させない。ウィンドウや描画のコードとゲームルールのコードを混在させない。

## コアゲームルール（仕様の正）

睡眠ゲージ (`sleep_gauge`) は次のルールで管理する。

- 範囲は 0〜1000（下限0でクランプ、上限1000を超えない）
- ミニゲーム（筋トレ／お風呂／サプリメント）に成功すると、成功難易度に応じた可変量だけゲージを減算できる
- ミニゲームへの挑戦は**任意**。何もしなくてもゲームは進行する（ミニゲームをやらない選択が常に成立する）
- ゲージが更新されるたびに（増加・減少どちらでも）**即座に**1000到達をチェックする。到達した瞬間に強制気絶＝ゲームオーバーとする
- その日ゲームオーバーにならなければ、1日の終わりに**現在のゲージ値がいくつであっても無条件で**-30する固定処理を1回だけ実行する（この処理自体では即時のゲームオーバー判定は発生しない想定だが、更新後の値は0未満にクランプする）
- 7日目の日末固定処理をゲームオーバーなしで通過したらクリア（勝利）

ゲージの値は専用のsetter経由でのみ変更できるようにし、そのsetter内で「1000到達判定」と「0未満のクランプ」を一元管理する。個々のミニゲームや日末処理の実装側で判定ロジックを重複させない。

## ミニゲームの共通インターフェース

新しいミニゲームを追加するときは `lib/minigames/` にクラスを1つ追加するだけで完結させる。各ミニゲームクラスは最低限、以下を実装する。

- `update(dt)` — フレームごとの更新
- `draw` — 描画
- `finished?` — ミニゲームが終了したか
- `succeeded?` — 成功したか（`finished?`がtrueの場合のみ意味を持つ）
- `gauge_reduction` — 成功時にゲージから減算する量（難易度・成功度に応じて可変）

本体側（`lib/states`）はこのインターフェースに従って登録されたミニゲームを呼び出すだけとし、個別のミニゲームの中身には関知しない。

## コーディング規約

- RuboCopのデフォルトルールに準拠（プロジェクト固有の除外は `.rubocop.yml` に明記してここに追記する）
- `lib/core` 配下の変更には対応するMinitestの追加・更新を必須とする（`ruby test/core/対象_test.rb`で実行し、パスすることを確認する）
- 1コミット1関心事。ミニゲーム1本の追加のようにスコープが明確なタスクは1コミットにまとめる
